import React from "react";

export default function StartSessionPage() {
  return (
    <section className="card" aria-label="Start session">
      <h1 style={{ marginTop: 0 }}>Start Session</h1>
      <p className="muted">
        Skeleton placeholder. The session timer (Pomodoro/custom intervals) will be implemented
        incrementally.
      </p>
    </section>
  );
}

